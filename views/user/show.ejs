<!DOCTYPE html>
<%- include('../partials/_head') %>

<body>
	<% if(user && userId){if(userId.toString() === user._id.toString()){ %> <%=
		user.username %> <%= userId %> <% } else{ %>
	<h3>Wrong User</h3>
	<% } } else { %>
	<h3>You must login</h3>
	<% } %>
</body>

</html>
